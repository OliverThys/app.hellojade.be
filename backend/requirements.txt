# ===========================================
# HelloJADE — Requirements
# Stack: Azure STT/TTS + Mistral API + Asterisk ARI + OVH SIP
# ===========================================

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg[binary]==3.2.3  # Driver synchrone pour Celery

# Redis & Celery
redis==5.0.8
celery==5.4.0
flower==2.0.1

# Validation & Settings
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==42.0.8
python3-saml==1.16.0

# ── IA — Azure Cognitive Services (STT + TTS) ──────────────────────────────
# Nécessite libssl et libs système sur Ubuntu :
#   sudo apt install libssl-dev libspeex-dev libazure-cognitive-services-speech
azure-cognitiveservices-speech==1.41.1
rapidfuzz==3.10.1

# ── IA — Mistral API (LLM principal, analyse médicale) ─────────────────────
mistralai==1.2.5

# ── Téléphonie — Asterisk ARI (WebSocket events + REST HTTP) ───────────────
websockets==13.1         # Connexion WS persistante vers Asterisk ARI events
# httpx ci-dessous pour les appels REST ARI

# Audio Processing
pydub==0.25.1
soundfile==0.12.1

# PDF Generation
reportlab==4.2.2
PyPDF2==3.0.1
weasyprint==62.3

# Documentation (génération .docx — scripts)
python-docx==1.1.2

# HTTP Client
httpx==0.27.2
aiofiles==24.1.0
requests==2.32.3

# Date & Time
python-dateutil==2.9.0
pytz==2024.2

# Email
aiosmtplib==3.0.2
email-validator==2.2.0

# SFTP (transport HL7 ORU vers Mirth)
paramiko==3.4.1

# Utils
python-json-logger==2.0.7
tenacity==9.0.0
click==8.1.7
itsdangerous==2.2.0

# Monitoring
prometheus-client==0.21.0
prometheus-fastapi-instrumentator==7.0.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0
faker==28.4.1

# Development
black==24.8.0
flake8==7.1.1
mypy==1.11.2
isort==5.13.2
pre-commit==3.8.0
ipython==8.27.0
